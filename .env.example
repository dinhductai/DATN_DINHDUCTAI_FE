# WebSocket URL for real-time notifications
# Dev: bỏ trống (dùng Vite proxy /ws → localhost:8080/ws)
# Production: set đầy đủ URL wss:// của API Gateway
# Ví dụ: VITE_WS_URL=wss://smart-schedule.com/ws
VITE_WS_URL=
